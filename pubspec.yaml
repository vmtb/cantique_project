name: cantique
description: Cantique Goun

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.0-69.2.beta <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_riverpod:
  cloud_firestore:
  firebase_core:
  firebase_auth:
  firebase_storage:


  form_validator:
  timer_controller:
  shared_preferences:
  flutter_flushbar:
  enum_to_string:
  uuid:
  google_fonts:
  cached_network_image:
  file_picker:
  audioplayers: ^0.20.1




dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  flutter_launcher_icons:

# The following section is specific to Flutter.
flutter:

  uses-material-design: true


  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

























